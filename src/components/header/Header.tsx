import AuthBtns from "../authBtns/AuthBtns";
import HeaderClient from "./HeaderClient";

const Header = () => {
  return (
    <HeaderClient>
      <AuthBtns />
    </HeaderClient>
  );
};

export default Header;
